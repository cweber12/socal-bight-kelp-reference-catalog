# Fetched sources: reproducible from src/fetch/ plus catalog/data-lock.json. Never tracked.
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Tests and tooling
.coverage
.coverage.*
.pytest_cache/
.ruff_cache/
htmlcov/

# Notebooks
.ipynb_checkpoints/
*.nbconvert.ipynb

# Agent scratch: session notes and review drafts, not repo documents.
Claude outputs/

# Agent worktrees: git worktree checkouts made in this clone, branch or detached.
# Ignoring them also keeps a nested checkout's Python out of the lint gate's file set.
.claude/worktrees/

# OS
Thumbs.db
desktop.ini
.DS_Store
